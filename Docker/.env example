# API KEYS AND CREDENTIALS
# Replace with your real API key for market data
# In our project we use Yahoo Finance prices using yfinance
MARKET_API_KEY= changeme



# DBT CONFIGURATION
# Path inside the Airflow container where your dbt profiles.yml is mounted
DBT_PROFILES_DIR=/opt/airflow/dbt

# Target profile name from profiles.yml (e.g., dev, prod)
DBT_TARGET=prod

# Selector to identify which models belong to the gold layer
# You can use a dbt tag (tag:gold) or path (path:models/marts/gold)
DBT_GOLD_SELECTOR=tag:gold



# AIRFLOW & GENERAL SETTINGS
# Timezone used for scheduling (affects cron expressions)
SCHEDULE_TZ=Europe/Tallinn

# Path inside Airflow container where your Python scripts are located
SCRIPTS_DIR=/opt/airflow/dags/scripts
